method:
  name: dann
  grl_max: 1.0               # max gradient-reversal strength (design study: 0.25 / 0.5 / 1.0)
  grl_gamma: 10.0            # alpha(p) = grl_max * (2 / (1 + exp(-gamma p)) - 1)
  domain_loss_weight: 1.0
  disc_hidden: 256
  disc_dropout: 0.5
output:
  run_name: dann